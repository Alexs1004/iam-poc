# Mini IAM Lab environment template (development only)
KEYCLOAK_ISSUER=http://localhost:8080/realms/demo
OIDC_CLIENT_ID=flask-app
OIDC_REDIRECT_URI=http://localhost:5000/callback
POST_LOGOUT_REDIRECT_URI=http://localhost:5000/
FLASK_SECRET=replace-me
# Uncomment if using a confidential client for the Flask app
#OIDC_CLIENT_SECRET=
# Toggle to true only when serving Flask over HTTPS
#FLASK_SESSION_COOKIE_SECURE=false
